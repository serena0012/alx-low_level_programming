#include <stdio.h>
/**
 *  main - print if the number is zero, positive or negative
 *  Description - using the main function
 *  Return: always 0.
 */
int main(void)
{
char c;
for (c = 'z'; c >= 'a'; c--)
putchar(c);
putchar('\n');
return (0);
}
